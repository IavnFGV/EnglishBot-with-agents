Read:
- context/englishbot_handoff.md
- context/current_state.md
- AGENTS.md

Goal:
Prepare the schema and persistence model for workbook-key-based teacher workspace import/export.

Context:
A corrected workbook spec has been accepted conceptually:
- workbook_key-first
- partial upsert
- no delete on missing rows
- no silent translation delete
- teacher workspace content only

However, current repository is not yet ready to implement this spec directly.

This slice is ONLY for schema and persistence preparation.

Task:
Implement the smallest possible schema-support for workbook keys and verify real persistence mappings.

Scope:

1. Add stable workbook keys for teacher-workspace content:
   - topics.workbook_key
   - learning_items.workbook_key

2. Enforce uniqueness:
   - workbook_key must be unique within workspace_id for each entity type

3. Support legacy rows:
   - existing topics and learning_items must be backfilled with generated workbook keys

4. Re-check and document actual persistence field mappings in the real repository:
   - do not assume fields such as `lemma`, `text`, `name`, or `title` unless they really exist
   - correct the workbook target spec if real schema names differ

5. Do NOT implement workbook import/export yet

Constraints:
- minimal schema change only
- no workbook UI
- no Telegram command changes
- no student workspace behavior changes
- no versioning
- no diff engine

Testing requirements:
- tests for workbook_key generation on new rows
- tests for migration/backfill of legacy rows
- tests for uniqueness within workspace
- tests for teacher workspace content paths still working after schema change

Documentation:
- update CHANGELOG.md
- update context/current_state.md
- record confirmed persistence mappings for the future workbook slice

Workflow:
1. architect
2. complexity_guard
3. coder
4. complexity_guard

Definition of done:
- topics and learning_items support stable workbook keys
- legacy rows are backfilled
- uniqueness is enforced
- real persistence mappings are confirmed from code
- repository is ready for workbook export/import implementation